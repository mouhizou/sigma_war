import 'package:flutter/material.dart';
class groupechatetaile extends StatelessWidget {
  const groupechatetaile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Column(
        children: [
          //pic and name and strick dayes
          Row(),
          //laste mesage and number of unreed messages
          Row(),
          //ranke and and time of the late message
          Row()
        ],
      ),
    );
  }
}
