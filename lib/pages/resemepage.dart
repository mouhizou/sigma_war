import 'package:flutter/material.dart';
class rsemepage extends StatelessWidget {
  const rsemepage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [
          //resume withe chart columns or a heatmap

          //persentage and score

          //liste of habits and there complition states

          //
        ],
      ),
    );
  }
}
